<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.travel.booking.dao.mybatis.UserMapper">

    <insert id="create" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (
        firstName,
        lastName,
        email,
        phoneNumber,
        dateOfBirth,
        createdAt,
        travel_booking_system_id
        )

        VALUES (
        #{firstName},
        #{lastName},
        #{email},
        #{phoneNumber},
        #{birthDate},
        #{createdAt},
        #{travelBookingSystem.id}
        )
    </insert>

    <select id="findBy" resultType="com.travel.booking.model.User">
        SELECT * FROM users
        WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.travel.booking.model.User">
        SELECT * FROM users
    </select>

    <update id="update">
        UPDATE users
        SET
        firstName = #{firstName},
        lastName = #{lastName},
        email = #{email},
        phoneNumber = #{phoneNumber}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM users
        WHERE id = #{id}
    </delete>

</mapper>