<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

    <properties resource="db.properties"/>

    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>

    <environments default="development">

        <environment id="development">

            <transactionManager type="JDBC"/>

            <dataSource type="POOLED">
                <property name="driver" value="${db.driver}"/>
                <property name="url" value="${db.url}"/>
                <property name="username" value="${db.username}"/>
                <property name="password" value="${db.password}"/>
            </dataSource>

        </environment>

    </environments>

    <mappers>
        <mapper resource="mapper/UserMapper.xml"/>
        <mapper resource="mapper/BookingMapper.xml"/>
        <mapper resource="mapper/DestinationMapper.xml"/>
        <mapper resource="mapper/FlightMapper.xml"/>
        <mapper resource="mapper/HotelMapper.xml"/>
        <mapper resource="mapper/PaymentMapper.xml"/>
        <mapper resource="mapper/ReviewMapper.xml"/>
        <mapper resource="mapper/TransportMapper.xml"/>
        <mapper resource="mapper/TravelBookingSystemMapper.xml"/>
        <mapper resource="mapper/TravelPackageMapper.xml"/>
        <mapper resource="mapper/TripMapper.xml"/>
    </mappers>

</configuration>